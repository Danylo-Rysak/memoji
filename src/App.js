import './App.css';
import Timer from "./components/Timer/Timer";

const App = () => {
  return (
        <main className="app_wrapper">
            <h1 className="app_title">Memoji</h1>
            <Timer/>
        </main>
    );
}

export default App;
